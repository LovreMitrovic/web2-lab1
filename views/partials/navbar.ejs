<nav class="navbar navbar-expand-lg navbar-light bg-dark justify-content-between">
    <a class="navbar-brand text-white" href="/competitions">Competitions</a>
    <div>
        <% if(user !== null){ %>
            <span class="text-white"><%=user.email%></span>
            <a class="btn btn-outline-secondary ml-4" href="/logout">Log out</a>
        <%}else{%>
            <a class="btn btn-outline-secondary" href="/login">Log in</a>
        <%}%>
    </div>
</nav>